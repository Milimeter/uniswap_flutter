/// Enum representing different types of coins.
enum CoinType {
  token,
  coin,
  nativeToken,
}

/// Enum representing different chain units.
enum ChainUnit {
  satoshi,
  ether,
  wei,
  matic,
}

enum PoolChain { bsc, eth }
